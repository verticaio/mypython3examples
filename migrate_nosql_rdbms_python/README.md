# Nosql to RDBMS Migration.
```
Example, For example  We suppose we have contact data in mongo and then ask from you we have to  migrate these datas  to RDBMS. For rdbms I chosen mysql for easy behaviour
Plan:
1. Analyzing  mongo data structure and create tables in rdbms for appropriate  to the mongo document structure(Table structure is given table-structure.sql file)
2. Create tables in rdbms
3. Start developing
```